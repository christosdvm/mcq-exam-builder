# Deployment guide

The reference deployment runs on Streamlit Community Cloud.

For local use, install Python 3.12, install dependencies from `requirements.txt`, install LibreOffice where legacy document conversion is required, then run `python -m streamlit run app.py`.

Deployment changes should pass the repository's GitHub Actions quality workflow before release.

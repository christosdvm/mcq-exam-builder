# Development workflow

Use short-lived branches, focused pull requests, synthetic fixtures, and CI before merging to `main`. Avoid committing real examinations, answer keys, student data, credentials, or other confidential material.

Production behavior changes should include regression coverage. Public documentation should describe tested behavior and explicit limitations without overstating compliance, confidentiality, or format coverage.

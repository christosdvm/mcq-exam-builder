# Quality and verification

The public snapshot is validated through GitHub Actions with Python compilation, focused static checks, dependency consistency/audit, and synthetic regression/compatibility tests.

The canonical private development repository carries the broader internal verification history. This public repository intentionally exposes a curated portfolio snapshot rather than the complete development history.

Tests provide regression evidence; they do not guarantee compatibility with every document layout or establish the academic correctness of question content.

[tool.ruff]
target-version = "py312"
line-length = 100
extend-exclude = [".venv"]

[tool.ruff.lint]
# Keep the first CI gate focused on defects rather than a repository-wide
# formatting rewrite. Broader style rules can be adopted module by module.
select = ["E9", "F63", "F7", "F82"]
